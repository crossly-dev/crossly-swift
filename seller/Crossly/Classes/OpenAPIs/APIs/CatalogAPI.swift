//
// CatalogAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class CatalogAPI {

    /**
     * enum for parameter namespace
     */
    public enum Namespace_getCatalogLookup: String, CaseIterable {
        case gtin = "gtin"
        case styleCode = "style_code"
        case legoSet = "lego_set"
        case tcgplayer = "tcgplayer"
        case discogs = "discogs"
        case asin = "asin"
    }

    /**
     Live Crossly offers for a product identifier (barcode, style code, LEGO set…).
     
     - parameter namespace: (query)  
     - parameter value: (query)  
     - parameter apiResponseQueue: The queue on which api response is dispatched.
     - parameter completion: completion handler to receive the data and the error objects
     */
    @discardableResult
    open class func getCatalogLookup(namespace: Namespace_getCatalogLookup, value: String, apiResponseQueue: DispatchQueue = CrosslyAPI.apiResponseQueue, completion: @escaping ((_ data: GetCatalogLookupResponse?, _ error: Error?) -> Void)) -> RequestTask {
        return getCatalogLookupWithRequestBuilder(namespace: namespace, value: value).execute(apiResponseQueue) { result in
            switch result {
            case let .success(response):
                completion(response.body, nil)
            case let .failure(error):
                completion(nil, error)
            }
        }
    }

    /**
     Live Crossly offers for a product identifier (barcode, style code, LEGO set…).
     - GET /v1/catalog/lookup
     - Identifier-first: a GTIN is validated against its GS1 check digit and every length is normalised to 14 digits before lookup, so a UPC-A and its EAN-13 twin resolve to the same product. There is no fuzzy fallback — an identifier we cannot validate returns nothing rather than a guess.
     - BASIC:
       - type: http
       - name: PersonalAccessToken
     - parameter namespace: (query)  
     - parameter value: (query)  
     - returns: RequestBuilder<GetCatalogLookupResponse> 
     */
    open class func getCatalogLookupWithRequestBuilder(namespace: Namespace_getCatalogLookup, value: String) -> RequestBuilder<GetCatalogLookupResponse> {
        let localVariablePath = "/v1/catalog/lookup"
        let localVariableURLString = CrosslyAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "namespace": namespace.encodeToJSON(),
            "value": value.encodeToJSON(),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<GetCatalogLookupResponse>.Type = CrosslyAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters)
    }
}
