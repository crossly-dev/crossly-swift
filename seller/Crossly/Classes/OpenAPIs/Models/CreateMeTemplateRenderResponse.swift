//
// CreateMeTemplateRenderResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateMeTemplateRenderResponse: Codable, JSONEncodable, Hashable {

    public enum TokensUsed: String, Codable, CaseIterable {
        case date = "date"
        case brand = "brand"
        case sku = "sku"
        case condition = "condition"
        case size = "size"
        case material = "material"
        case style = "style"
        case color = "color"
        case price = "price"
        case category = "category"
        case year = "year"
        case model = "model"
        case itemName = "item_name"
        case month = "month"
    }
    public enum MissingTokens: String, Codable, CaseIterable {
        case date = "date"
        case brand = "brand"
        case sku = "sku"
        case condition = "condition"
        case size = "size"
        case material = "material"
        case style = "style"
        case color = "color"
        case price = "price"
        case category = "category"
        case year = "year"
        case model = "model"
        case itemName = "item_name"
        case month = "month"
    }
    public var title: String
    public var description: String
    public var tokensUsed: [TokensUsed]
    public var missingTokens: [MissingTokens]

    public init(title: String, description: String, tokensUsed: [TokensUsed], missingTokens: [MissingTokens]) {
        self.title = title
        self.description = description
        self.tokensUsed = tokensUsed
        self.missingTokens = missingTokens
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case title
        case description
        case tokensUsed
        case missingTokens
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(title, forKey: .title)
        try container.encode(description, forKey: .description)
        try container.encode(tokensUsed, forKey: .tokensUsed)
        try container.encode(missingTokens, forKey: .missingTokens)
    }
}

