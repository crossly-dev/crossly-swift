//
// GetConnectionHealthResponseExtension.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetConnectionHealthResponseExtension: Codable, JSONEncodable, Hashable {

    /** Heartbeat within the last 15 minutes. False is NOT proof of breakage — a closed browser looks the same — so it is always paired with the durable timestamp below. */
    public var online: Bool
    /** Newest browser push across every account. The durable answer to \"when did the extension last do anything\", surviving a Redis flush. */
    public var lastBrowserPushAt: String?
    public var lastBrowserPushAgo: String
    /** Highest version any platform's browser report carried, or null when the extension has never told us (it does not send one today — see the service README notes in routes/extension-health.ts). */
    public var reportedVersion: String?

    public init(online: Bool, lastBrowserPushAt: String? = nil, lastBrowserPushAgo: String, reportedVersion: String? = nil) {
        self.online = online
        self.lastBrowserPushAt = lastBrowserPushAt
        self.lastBrowserPushAgo = lastBrowserPushAgo
        self.reportedVersion = reportedVersion
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case online
        case lastBrowserPushAt
        case lastBrowserPushAgo
        case reportedVersion
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(online, forKey: .online)
        try container.encodeIfPresent(lastBrowserPushAt, forKey: .lastBrowserPushAt)
        try container.encode(lastBrowserPushAgo, forKey: .lastBrowserPushAgo)
        try container.encodeIfPresent(reportedVersion, forKey: .reportedVersion)
    }
}

