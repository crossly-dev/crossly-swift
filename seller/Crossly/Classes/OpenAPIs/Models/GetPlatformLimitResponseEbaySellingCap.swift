//
// GetPlatformLimitResponseEbaySellingCap.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** eBay-imposed monthly selling cap (quantity + $$) fetched from Trading API GetMyeBaySelling. These are HARD blocks — past them, &#x60;publishOffer&#x60; returns an error. Distinct from the free-tier &#x60;limit&#x60; (which is just a fee threshold). Null when seller has no caps (established accounts) or the Trading call failed. */
public struct GetPlatformLimitResponseEbaySellingCap: Codable, JSONEncodable, Hashable {

    /** Monthly $$ ceiling — null when seller has no $$ cap set. */
    public var amountLimit: Double?
    public var amountUsed: Double?
    /** Monthly item-count ceiling — null when seller has no qty cap. */
    public var quantityLimit: Double?
    public var quantityUsed: Double?
    public var currency: String
    /** ISO timestamp this was fetched (used for cache freshness display). */
    public var fetchedAt: String

    public init(amountLimit: Double? = nil, amountUsed: Double? = nil, quantityLimit: Double? = nil, quantityUsed: Double? = nil, currency: String, fetchedAt: String) {
        self.amountLimit = amountLimit
        self.amountUsed = amountUsed
        self.quantityLimit = quantityLimit
        self.quantityUsed = quantityUsed
        self.currency = currency
        self.fetchedAt = fetchedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case amountLimit
        case amountUsed
        case quantityLimit
        case quantityUsed
        case currency
        case fetchedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(amountLimit, forKey: .amountLimit)
        try container.encodeIfPresent(amountUsed, forKey: .amountUsed)
        try container.encodeIfPresent(quantityLimit, forKey: .quantityLimit)
        try container.encodeIfPresent(quantityUsed, forKey: .quantityUsed)
        try container.encode(currency, forKey: .currency)
        try container.encode(fetchedAt, forKey: .fetchedAt)
    }
}

