//
// CreateCbxRateQuoteResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateCbxRateQuoteResponse: Codable, JSONEncodable, Hashable {

    public enum Source: String, Codable, CaseIterable {
        case tier = "tier"
        case base = "base"
        case boost = "boost"
    }
    public var rateBps: Double
    public var cents: Double
    public var maturationDays: Double
    public var source: Source
    public var tierSlug: String?
    public var boostName: String?
    public var stakeBoostBps: Double
    public var clamped: Bool

    public init(rateBps: Double, cents: Double, maturationDays: Double, source: Source, tierSlug: String? = nil, boostName: String? = nil, stakeBoostBps: Double, clamped: Bool) {
        self.rateBps = rateBps
        self.cents = cents
        self.maturationDays = maturationDays
        self.source = source
        self.tierSlug = tierSlug
        self.boostName = boostName
        self.stakeBoostBps = stakeBoostBps
        self.clamped = clamped
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case rateBps
        case cents
        case maturationDays
        case source
        case tierSlug
        case boostName
        case stakeBoostBps
        case clamped
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(rateBps, forKey: .rateBps)
        try container.encode(cents, forKey: .cents)
        try container.encode(maturationDays, forKey: .maturationDays)
        try container.encode(source, forKey: .source)
        try container.encodeIfPresent(tierSlug, forKey: .tierSlug)
        try container.encodeIfPresent(boostName, forKey: .boostName)
        try container.encode(stakeBoostBps, forKey: .stakeBoostBps)
        try container.encode(clamped, forKey: .clamped)
    }
}

