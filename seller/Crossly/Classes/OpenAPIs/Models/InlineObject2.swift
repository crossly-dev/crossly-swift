//
// InlineObject2.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct InlineObject2: Codable, JSONEncodable, Hashable {

    public enum Action: String, Codable, CaseIterable {
        case accept = "accept"
        case decline = "decline"
        case counter = "counter"
    }
    public var action: Action
    /** Required when action is \"counter\". Your asking price for the set. */
    public var counterCents: Int?

    public init(action: Action, counterCents: Int? = nil) {
        self.action = action
        self.counterCents = counterCents
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case action
        case counterCents
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(action, forKey: .action)
        try container.encodeIfPresent(counterCents, forKey: .counterCents)
    }
}

