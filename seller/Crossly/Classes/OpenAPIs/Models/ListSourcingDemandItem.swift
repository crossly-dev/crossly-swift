//
// ListSourcingDemandItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ListSourcingDemandItem: Codable, JSONEncodable, Hashable {

    public var namespace: String
    public var identifierValue: String
    /** How many times anybody looked, in the window. */
    public var looks: Double
    /** How many of those we had nothing for. */
    public var misses: Double
    /** Distinct retailers it was seen on — breadth, not just volume. */
    public var retailers: Double
    /** What the retailers were charging, median of what we saw. */
    public var medianPageCents: Double?
    public var lastSeenAt: Date

    public init(namespace: String, identifierValue: String, looks: Double, misses: Double, retailers: Double, medianPageCents: Double? = nil, lastSeenAt: Date) {
        self.namespace = namespace
        self.identifierValue = identifierValue
        self.looks = looks
        self.misses = misses
        self.retailers = retailers
        self.medianPageCents = medianPageCents
        self.lastSeenAt = lastSeenAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case namespace
        case identifierValue
        case looks
        case misses
        case retailers
        case medianPageCents
        case lastSeenAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(namespace, forKey: .namespace)
        try container.encode(identifierValue, forKey: .identifierValue)
        try container.encode(looks, forKey: .looks)
        try container.encode(misses, forKey: .misses)
        try container.encode(retailers, forKey: .retailers)
        try container.encodeIfPresent(medianPageCents, forKey: .medianPageCents)
        try container.encode(lastSeenAt, forKey: .lastSeenAt)
    }
}

