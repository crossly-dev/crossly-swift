//
// InlineObject3.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct InlineObject3: Codable, JSONEncodable, Hashable {

    public enum Environment: String, Codable, CaseIterable {
        case live = "live"
        case test = "test"
    }
    /** Which site this is for, e.g. \"my Squarespace shop\". */
    public var name: String
    /** Origins permitted to use this key, e.g. [\"https://shop.example.com\"]. LEAVE EMPTY to allow any origin — necessary when embedding on a platform whose domain you do not control, and the reason this is not locked by default. */
    public var allowedOrigins: [String]?
    public var environment: Environment? = .live

    public init(name: String, allowedOrigins: [String]? = nil, environment: Environment? = .live) {
        self.name = name
        self.allowedOrigins = allowedOrigins
        self.environment = environment
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case allowedOrigins
        case environment
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(allowedOrigins, forKey: .allowedOrigins)
        try container.encodeIfPresent(environment, forKey: .environment)
    }
}

