//
// CreateMagicScanSynthesizeResponsePayload.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateMagicScanSynthesizeResponsePayload: Codable, JSONEncodable, Hashable {

    public var title: String
    public var description: String
    public var brand: String?
    public var grading: CreateMagicScanSynthesizeResponsePayloadGrading?
    public var color: String?
    public var material: String?
    public var size: String?
    /** Poshmark/Vestiaire size system — \"US\", \"EU\", \"UK\", etc. */
    public var sizeSystem: String?
    public var weightOz: Double?
    public var dimensions: CreateMagicScanSynthesizeResponsePayloadDimensions?
    /** Echoed user-selected price (cents). Set on every synthesize call  so the master form's defaultPrice input gets populated regardless  of which path (AI or heuristic) generated the rest. */
    public var priceCents: Double?
    /** The seller's own words about this item — the pre-scan hint, or the  post-scan note that supersedes it (see synthesize/seller-note.ts).  Carried on the payload so the TAXONOMY resolvers see it too: category  and facet picking happen in their own LLM calls, which never saw the  note even after the main synthesis prompt did. A seller correcting the  variant was still getting the category of the wrong one. */
    public var sellerNote: String?
    /** Comp price band (cents), outlier-trimmed — low/median/high across the  matched comps. The UI surfaces the range so the seller prices  strategically instead of trusting one number; priceCents defaults to  the median when the seller hasn't picked. */
    public var priceLowCents: Double?
    public var priceMedianCents: Double?
    public var priceHighCents: Double?
    /** Echoed seller-supplied listable-unit count from Magic List. This is  the listing's ADVERTISED stock (how many of this listing to sell).  Hydrates the form's `quantity` field. Undefined = leave form default (1). */
    public var quantity: Double?
    /** Physical units the seller actually has in stock — becomes the auto-  created inventory item's quantity/quantityAvailable. Distinct from  `quantity` (advertised stock) and `unitsPerListing` (composition).  Undefined = fall back to `quantity`. */
    public var inventoryQuantity: Double?
    /** How many physical units are bundled inside ONE listing (composition;  e.g. a pack of 4). Becomes listing_inventory_items.quantity so the  multi-channel fulfillable = floor(inventoryQuantity / unitsPerListing).  Undefined = 1. */
    public var unitsPerListing: Double?
    /** What the seller paid, per physical unit, in cents — the auto-created  inventory item's `costBasisCents`. Nothing wrote that column from any  create path, so every Magic List item had a NULL cost basis and every  sale of one reported its full sale price as profit. Undefined = unknown,  which stays NULL (0 would be a claim that the item was free). */
    public var costBasisCents: Double?
    public var chosenDimensions: CreateMagicScanSynthesizeResponsePayloadChosenDimensions?
    /** Every photo the seller uploaded for the source scan, primary  first. The NewListingPage hydrator drops these straight into the  form's images state so the seller doesn't have to re-upload. */
    public var imageUrls: [String]?
    public var category: CreateMagicScanSynthesizeResponsePayloadCategory?
    /** eBay item-specifics: Department/Gender/Style/Pattern/Type. These  drive the eBay-required aspects on the form's Item Details  section. Mostly only eBay matches have them. */
    public var department: String?
    public var gender: String?
    public var style: String?
    public var pattern: String?
    public var itemType: String?
    /** Universal product code, when eBay's enriched detail surfaced it.  Hydrates the master form's UPC/GTIN field. */
    public var upc: String?
    public var handlingTimeDays: String?
    public var returnWindowDays: String?
    public var tags: [String]?
    /** Master-level item specifics (JSON Record<string,string[]>). Item  specifics are now master-owned + fanned to every aspect platform, so the  form's shared Item-specifics editor prefills from this. Mirrored from the  richest resolved per-platform specifics blob (eBay after full-aspect  enrichment). Undefined = no specifics. */
    public var itemSpecifics: String?
    /** Per-platform required-fields map — slotted directly into  platformOverrides when the form hydrates. */
    public var perPlatformOverrides: AnyCodable
    /** AI's confidence in the synthesis (0-1). Surfaced to the seller  so they can decide whether to skim or trust + click. */
    public var confidence: Double
    /** AI's free-text rationale for the merge — what it pulled from  where. Helps the seller spot a bad merge before listing. */
    public var notes: String?
    public var sectionApplicability: CreateMagicScanSynthesizeResponsePayloadSectionApplicability?

    public init(title: String, description: String, brand: String? = nil, grading: CreateMagicScanSynthesizeResponsePayloadGrading? = nil, color: String? = nil, material: String? = nil, size: String? = nil, sizeSystem: String? = nil, weightOz: Double? = nil, dimensions: CreateMagicScanSynthesizeResponsePayloadDimensions? = nil, priceCents: Double? = nil, sellerNote: String? = nil, priceLowCents: Double? = nil, priceMedianCents: Double? = nil, priceHighCents: Double? = nil, quantity: Double? = nil, inventoryQuantity: Double? = nil, unitsPerListing: Double? = nil, costBasisCents: Double? = nil, chosenDimensions: CreateMagicScanSynthesizeResponsePayloadChosenDimensions? = nil, imageUrls: [String]? = nil, category: CreateMagicScanSynthesizeResponsePayloadCategory? = nil, department: String? = nil, gender: String? = nil, style: String? = nil, pattern: String? = nil, itemType: String? = nil, upc: String? = nil, handlingTimeDays: String? = nil, returnWindowDays: String? = nil, tags: [String]? = nil, itemSpecifics: String? = nil, perPlatformOverrides: AnyCodable, confidence: Double, notes: String? = nil, sectionApplicability: CreateMagicScanSynthesizeResponsePayloadSectionApplicability? = nil) {
        self.title = title
        self.description = description
        self.brand = brand
        self.grading = grading
        self.color = color
        self.material = material
        self.size = size
        self.sizeSystem = sizeSystem
        self.weightOz = weightOz
        self.dimensions = dimensions
        self.priceCents = priceCents
        self.sellerNote = sellerNote
        self.priceLowCents = priceLowCents
        self.priceMedianCents = priceMedianCents
        self.priceHighCents = priceHighCents
        self.quantity = quantity
        self.inventoryQuantity = inventoryQuantity
        self.unitsPerListing = unitsPerListing
        self.costBasisCents = costBasisCents
        self.chosenDimensions = chosenDimensions
        self.imageUrls = imageUrls
        self.category = category
        self.department = department
        self.gender = gender
        self.style = style
        self.pattern = pattern
        self.itemType = itemType
        self.upc = upc
        self.handlingTimeDays = handlingTimeDays
        self.returnWindowDays = returnWindowDays
        self.tags = tags
        self.itemSpecifics = itemSpecifics
        self.perPlatformOverrides = perPlatformOverrides
        self.confidence = confidence
        self.notes = notes
        self.sectionApplicability = sectionApplicability
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case title
        case description
        case brand
        case grading
        case color
        case material
        case size
        case sizeSystem
        case weightOz
        case dimensions
        case priceCents
        case sellerNote
        case priceLowCents
        case priceMedianCents
        case priceHighCents
        case quantity
        case inventoryQuantity
        case unitsPerListing
        case costBasisCents
        case chosenDimensions
        case imageUrls
        case category
        case department
        case gender
        case style
        case pattern
        case itemType
        case upc
        case handlingTimeDays
        case returnWindowDays
        case tags
        case itemSpecifics
        case perPlatformOverrides
        case confidence
        case notes
        case sectionApplicability
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(title, forKey: .title)
        try container.encode(description, forKey: .description)
        try container.encodeIfPresent(brand, forKey: .brand)
        try container.encodeIfPresent(grading, forKey: .grading)
        try container.encodeIfPresent(color, forKey: .color)
        try container.encodeIfPresent(material, forKey: .material)
        try container.encodeIfPresent(size, forKey: .size)
        try container.encodeIfPresent(sizeSystem, forKey: .sizeSystem)
        try container.encodeIfPresent(weightOz, forKey: .weightOz)
        try container.encodeIfPresent(dimensions, forKey: .dimensions)
        try container.encodeIfPresent(priceCents, forKey: .priceCents)
        try container.encodeIfPresent(sellerNote, forKey: .sellerNote)
        try container.encodeIfPresent(priceLowCents, forKey: .priceLowCents)
        try container.encodeIfPresent(priceMedianCents, forKey: .priceMedianCents)
        try container.encodeIfPresent(priceHighCents, forKey: .priceHighCents)
        try container.encodeIfPresent(quantity, forKey: .quantity)
        try container.encodeIfPresent(inventoryQuantity, forKey: .inventoryQuantity)
        try container.encodeIfPresent(unitsPerListing, forKey: .unitsPerListing)
        try container.encodeIfPresent(costBasisCents, forKey: .costBasisCents)
        try container.encodeIfPresent(chosenDimensions, forKey: .chosenDimensions)
        try container.encodeIfPresent(imageUrls, forKey: .imageUrls)
        try container.encodeIfPresent(category, forKey: .category)
        try container.encodeIfPresent(department, forKey: .department)
        try container.encodeIfPresent(gender, forKey: .gender)
        try container.encodeIfPresent(style, forKey: .style)
        try container.encodeIfPresent(pattern, forKey: .pattern)
        try container.encodeIfPresent(itemType, forKey: .itemType)
        try container.encodeIfPresent(upc, forKey: .upc)
        try container.encodeIfPresent(handlingTimeDays, forKey: .handlingTimeDays)
        try container.encodeIfPresent(returnWindowDays, forKey: .returnWindowDays)
        try container.encodeIfPresent(tags, forKey: .tags)
        try container.encodeIfPresent(itemSpecifics, forKey: .itemSpecifics)
        try container.encode(perPlatformOverrides, forKey: .perPlatformOverrides)
        try container.encode(confidence, forKey: .confidence)
        try container.encodeIfPresent(notes, forKey: .notes)
        try container.encodeIfPresent(sectionApplicability, forKey: .sectionApplicability)
    }
}

