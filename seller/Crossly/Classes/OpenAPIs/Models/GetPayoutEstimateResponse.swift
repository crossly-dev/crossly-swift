//
// GetPayoutEstimateResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetPayoutEstimateResponse: Codable, JSONEncodable, Hashable {

    public enum FeeSource: String, Codable, CaseIterable {
        case exact = "exact"
        case estimated = "estimated"
    }
    public enum ShippingSource: String, Codable, CaseIterable {
        case _none = "none"
        case unknown = "unknown"
        case quoted = "quoted"
        case platformLabel = "platform_label"
        case buyerPaid = "buyer_paid"
    }
    public var platform: String
    public var grossCents: Double
    public var feeCents: Double
    public var feeSource: FeeSource
    public var shippingCents: Double
    public var shippingSource: ShippingSource
    public var netCents: Double
    /** Net as a percentage of gross, for comparing across price points. */
    public var takeHomePct: Double
    /** What we assumed, in the seller's words. Never empty when we guessed. */
    public var assumptions: [String]

    public init(platform: String, grossCents: Double, feeCents: Double, feeSource: FeeSource, shippingCents: Double, shippingSource: ShippingSource, netCents: Double, takeHomePct: Double, assumptions: [String]) {
        self.platform = platform
        self.grossCents = grossCents
        self.feeCents = feeCents
        self.feeSource = feeSource
        self.shippingCents = shippingCents
        self.shippingSource = shippingSource
        self.netCents = netCents
        self.takeHomePct = takeHomePct
        self.assumptions = assumptions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case platform
        case grossCents
        case feeCents
        case feeSource
        case shippingCents
        case shippingSource
        case netCents
        case takeHomePct
        case assumptions
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(platform, forKey: .platform)
        try container.encode(grossCents, forKey: .grossCents)
        try container.encode(feeCents, forKey: .feeCents)
        try container.encode(feeSource, forKey: .feeSource)
        try container.encode(shippingCents, forKey: .shippingCents)
        try container.encode(shippingSource, forKey: .shippingSource)
        try container.encode(netCents, forKey: .netCents)
        try container.encode(takeHomePct, forKey: .takeHomePct)
        try container.encode(assumptions, forKey: .assumptions)
    }
}

