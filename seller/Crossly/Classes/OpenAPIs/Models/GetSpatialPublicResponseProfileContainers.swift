//
// GetSpatialPublicResponseProfileContainers.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetSpatialPublicResponseProfileContainers: Codable, JSONEncodable, Hashable {

    /** Stable key. Persisted on every placement — never renamed. */
    public var key: String
    /** What the seller calls it. \"Page 3\", \"Case\", \"Long box\". */
    public var label: String
    /** Which hand-authored mesh renders this container. A small closed set — the  art budget lives here and nowhere else. */
    public var model: String
    /** Outer bounds, for packing containers into a room. */
    public var size: AnyCodable
    public var slots: AnyCodable

    public init(key: String, label: String, model: String, size: AnyCodable, slots: AnyCodable) {
        self.key = key
        self.label = label
        self.model = model
        self.size = size
        self.slots = slots
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case key
        case label
        case model
        case size
        case slots
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(key, forKey: .key)
        try container.encode(label, forKey: .label)
        try container.encode(model, forKey: .model)
        try container.encode(size, forKey: .size)
        try container.encode(slots, forKey: .slots)
    }
}

