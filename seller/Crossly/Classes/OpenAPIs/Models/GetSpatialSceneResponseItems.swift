//
// GetSpatialSceneResponseItems.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetSpatialSceneResponseItems: Codable, JSONEncodable, Hashable {

    public var id: String
    public var unitId: String?
    public var title: String
    public var imageUrl: String?
    /** A small copy of `imageUrl`, when the catalog has one.    The room binds this for everything except the few items you are standing  in front of. A 600x600 original costs 1.83 MiB of VRAM; a 150x150 thumb  costs 0.11 MiB, and at more than a couple of metres they are the same  handful of pixels on screen. Null when the catalog never made one, which  the renderer treats as \"use the original\" rather than as \"draw nothing\". */
    public var thumbUrl: String?
    public var size: GetSpatialSceneResponseSize?
    /** Cents the seller paid. Drives the `value` overlay and capital density. */
    public var costCents: Double?
    /** Days since first listed anywhere. Drives the `aging` overlay. */
    public var ageDays: Double?
    public var status: String
    /** Free-text or structured location, when the unit has one. */
    public var location: String?
    /** True when this row resolved to a catalog product. */
    public var marketTagged: Bool

    public init(id: String, unitId: String? = nil, title: String, imageUrl: String? = nil, thumbUrl: String? = nil, size: GetSpatialSceneResponseSize? = nil, costCents: Double? = nil, ageDays: Double? = nil, status: String, location: String? = nil, marketTagged: Bool) {
        self.id = id
        self.unitId = unitId
        self.title = title
        self.imageUrl = imageUrl
        self.thumbUrl = thumbUrl
        self.size = size
        self.costCents = costCents
        self.ageDays = ageDays
        self.status = status
        self.location = location
        self.marketTagged = marketTagged
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case unitId
        case title
        case imageUrl
        case thumbUrl
        case size
        case costCents
        case ageDays
        case status
        case location
        case marketTagged
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(unitId, forKey: .unitId)
        try container.encode(title, forKey: .title)
        try container.encodeIfPresent(imageUrl, forKey: .imageUrl)
        try container.encodeIfPresent(thumbUrl, forKey: .thumbUrl)
        try container.encodeIfPresent(size, forKey: .size)
        try container.encodeIfPresent(costCents, forKey: .costCents)
        try container.encodeIfPresent(ageDays, forKey: .ageDays)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(location, forKey: .location)
        try container.encode(marketTagged, forKey: .marketTagged)
    }
}

