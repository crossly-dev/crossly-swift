//
// ListSpatialPublicItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ListSpatialPublicItem: Codable, JSONEncodable, Hashable {

    public var name: String
    public var publicSlug: String
    public var categorySlug: String
    /** Unsold stock that lands in this room. The same predicate the room uses. */
    public var itemCount: Double
    /** How many of those a visitor could buy right now. */
    public var forSaleCount: Double
    /** The cheapest and dearest thing for sale, in cents.  A BAND, deliberately, and never a quote: `listPublicSceneOffers` is the only authority on what a given object costs. Null when nothing is for sale — zero would read as free. */
    public var priceFromCents: Double?
    public var priceToCents: Double?
    /** Up to PREVIEW_IMAGES item images. Catalog art first, seller photo else. */
    public var previewImages: [String]
    /** Last time the room itself changed. ISO, or null if the row has no date. */
    public var updatedAt: String?

    public init(name: String, publicSlug: String, categorySlug: String, itemCount: Double, forSaleCount: Double, priceFromCents: Double? = nil, priceToCents: Double? = nil, previewImages: [String], updatedAt: String? = nil) {
        self.name = name
        self.publicSlug = publicSlug
        self.categorySlug = categorySlug
        self.itemCount = itemCount
        self.forSaleCount = forSaleCount
        self.priceFromCents = priceFromCents
        self.priceToCents = priceToCents
        self.previewImages = previewImages
        self.updatedAt = updatedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case publicSlug
        case categorySlug
        case itemCount
        case forSaleCount
        case priceFromCents
        case priceToCents
        case previewImages
        case updatedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(publicSlug, forKey: .publicSlug)
        try container.encode(categorySlug, forKey: .categorySlug)
        try container.encode(itemCount, forKey: .itemCount)
        try container.encode(forSaleCount, forKey: .forSaleCount)
        try container.encodeIfPresent(priceFromCents, forKey: .priceFromCents)
        try container.encodeIfPresent(priceToCents, forKey: .priceToCents)
        try container.encode(previewImages, forKey: .previewImages)
        try container.encodeIfPresent(updatedAt, forKey: .updatedAt)
    }
}

