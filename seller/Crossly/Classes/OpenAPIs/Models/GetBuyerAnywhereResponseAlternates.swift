//
// GetBuyerAnywhereResponseAlternates.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetBuyerAnywhereResponseAlternates: Codable, JSONEncodable, Hashable {

    public var storeId: String
    /** The retailer's hostname, e.g. `rei.com`. */
    public var host: String
    public var storeName: String
    public var title: String
    public var priceCents: Double
    /** Null = UNKNOWN, never free. */
    public var shippingCents: Double?
    public var currency: String
    public var condition: String?
    public var url: String
    public var imageUrl: String?
    /** What the buyer gets back, in cents, if they buy through us.  Shown because a cashback figure the buyer cannot see is a figure they have no reason to believe. Derived from the store's rate, never stored per offer — rates change and a copied one goes stale silently. */
    public var buyerCashbackCents: Double
    /** Item + shipping when known; item alone otherwise. See `shippingUnknown`. */
    public var deliveredCents: Double
    public var shippingUnknown: Bool

    public init(storeId: String, host: String, storeName: String, title: String, priceCents: Double, shippingCents: Double? = nil, currency: String, condition: String? = nil, url: String, imageUrl: String? = nil, buyerCashbackCents: Double, deliveredCents: Double, shippingUnknown: Bool) {
        self.storeId = storeId
        self.host = host
        self.storeName = storeName
        self.title = title
        self.priceCents = priceCents
        self.shippingCents = shippingCents
        self.currency = currency
        self.condition = condition
        self.url = url
        self.imageUrl = imageUrl
        self.buyerCashbackCents = buyerCashbackCents
        self.deliveredCents = deliveredCents
        self.shippingUnknown = shippingUnknown
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case storeId
        case host
        case storeName
        case title
        case priceCents
        case shippingCents
        case currency
        case condition
        case url
        case imageUrl
        case buyerCashbackCents
        case deliveredCents
        case shippingUnknown
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(storeId, forKey: .storeId)
        try container.encode(host, forKey: .host)
        try container.encode(storeName, forKey: .storeName)
        try container.encode(title, forKey: .title)
        try container.encode(priceCents, forKey: .priceCents)
        try container.encodeIfPresent(shippingCents, forKey: .shippingCents)
        try container.encode(currency, forKey: .currency)
        try container.encodeIfPresent(condition, forKey: .condition)
        try container.encode(url, forKey: .url)
        try container.encodeIfPresent(imageUrl, forKey: .imageUrl)
        try container.encode(buyerCashbackCents, forKey: .buyerCashbackCents)
        try container.encode(deliveredCents, forKey: .deliveredCents)
        try container.encode(shippingUnknown, forKey: .shippingUnknown)
    }
}

