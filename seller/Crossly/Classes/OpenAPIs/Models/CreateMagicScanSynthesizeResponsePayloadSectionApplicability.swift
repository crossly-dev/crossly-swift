//
// CreateMagicScanSynthesizeResponsePayloadSectionApplicability.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Deterministic (NO-AI) applicability hint for the heavyweight optional  form sections (vehicle compatibility, EU EPR / energy label, hazmat).  Computed from the resolved category + seller region via  &#x60;applicableOptionalSections&#x60;; the form pre-checks each section&#39;s  \&quot;this item needs …\&quot; toggle from it. Additive + best-effort. */
public struct CreateMagicScanSynthesizeResponsePayloadSectionApplicability: Codable, JSONEncodable, Hashable {

    /** eBay Motors / Parts vehicle-fitment (compatibilityList). */
    public var vehicleCompat: Bool
    /** EU/UK Extended Producer Responsibility (packaging/e-waste schemes). */
    public var epr: Bool
    /** EU energy-efficiency label (fridges, TVs, lighting, appliances). */
    public var energyLabel: Bool
    /** Hazmat classification (battery/aerosol/flammable/liquid/chemical). */
    public var hazmat: Bool

    public init(vehicleCompat: Bool, epr: Bool, energyLabel: Bool, hazmat: Bool) {
        self.vehicleCompat = vehicleCompat
        self.epr = epr
        self.energyLabel = energyLabel
        self.hazmat = hazmat
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case vehicleCompat
        case epr
        case energyLabel
        case hazmat
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(vehicleCompat, forKey: .vehicleCompat)
        try container.encode(epr, forKey: .epr)
        try container.encode(energyLabel, forKey: .energyLabel)
        try container.encode(hazmat, forKey: .hazmat)
    }
}

