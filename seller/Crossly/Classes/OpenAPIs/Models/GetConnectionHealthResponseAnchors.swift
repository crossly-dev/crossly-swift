//
// GetConnectionHealthResponseAnchors.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetConnectionHealthResponseAnchors: Codable, JSONEncodable, Hashable {

    /** What we were looking for. Empty ⇒ this platform is unmonitored. */
    public var expected: [String]
    public var observations: [AnyCodable]
    /** Name found carrying a non-empty value — the only honest \"logged in\". */
    public var present: [String]
    /** Name found, value is the empty string. The Whatnot class. */
    public var empty: [String]
    /** Name not in the jar at all. */
    public var missing: [String]
    public var cookieCount: Double
    /** Cookie names actually in the jar, truncated. This is the payload that turns \"anchors missing\" into a diagnosis: if the jar holds 30 cookies and none are ours, a rename is the likely story; if it holds three device cookies, the browser is signed out. NAMES ONLY — never values. */
    public var observedCookieNames: [String]

    public init(expected: [String], observations: [AnyCodable], present: [String], empty: [String], missing: [String], cookieCount: Double, observedCookieNames: [String]) {
        self.expected = expected
        self.observations = observations
        self.present = present
        self.empty = empty
        self.missing = missing
        self.cookieCount = cookieCount
        self.observedCookieNames = observedCookieNames
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case expected
        case observations
        case present
        case empty
        case missing
        case cookieCount
        case observedCookieNames
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(expected, forKey: .expected)
        try container.encode(observations, forKey: .observations)
        try container.encode(present, forKey: .present)
        try container.encode(empty, forKey: .empty)
        try container.encode(missing, forKey: .missing)
        try container.encode(cookieCount, forKey: .cookieCount)
        try container.encode(observedCookieNames, forKey: .observedCookieNames)
    }
}

