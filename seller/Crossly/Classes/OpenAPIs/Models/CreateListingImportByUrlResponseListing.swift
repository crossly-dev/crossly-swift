//
// CreateListingImportByUrlResponseListing.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateListingImportByUrlResponseListing: Codable, JSONEncodable, Hashable {

    public var platformListingId: String
    public var title: String
    public var description: String?
    public var price: Double?
    public var images: [String]?
    public var brand: String?
    public var condition: String?
    public var size: String?
    /** Color(s) the platform's own response exposes (e.g. Poshmark's  `colors` array). Only set when actually present in the scrape. */
    public var color: [String]?
    public var sku: String?
    public var url: String?
    /** The platform's OWN category, as a top-down path (e.g. Poshmark's  [\"Kids\", \"Toys\", \"Dolls & Accessories\"] from department → category →  feature). Only set when the platform's list/scrape response actually  carries this — never guessed. Mapped onto Crossly's category.main/  sub/sub2 (listings) or categoryMain/categorySub (inventory_items) at  create/enrich time. */
    public var category: [String]?
    /** The platform's raw numeric category id, same space as  overrides.<platform>.categoryId — distinct from `category`'s  human-readable path. Only set by platforms whose id space is directly  comparable to what we publish (currently eBay's drift-check second  call — see diff-fields/second-call.ts's ebayRemoteFields). eBay's own  CategoryName wording/depth is a different vocabulary from Crossly's  master taxonomy breadcrumb and will essentially never string-match  it, so drift-detection compares ids instead of names. */
    public var categoryId: String?
    /** Tag-like strings the platform's own response exposes (e.g. Poshmark's  marketing \"experience\" tags). Distinct from a full search-tag  generator — just whatever real tag data the scrape already carries. */
    public var tags: [String]?
    /** Structured item aspects — the same shape the listing form's Item  Details section captures (and templates already persist), now sourced  from the platform's own data instead of only manual entry. Only set  when the platform's response genuinely carries a semantically-matching  field (eBay item specifics, Facebook attributes, Poshmark catalog,  Depop's detail-call ride-along) — never derived or guessed. */
    public var material: String?
    public var style: String?
    public var pattern: String?
    public var department: String?
    public var gender: String?
    public var itemType: String?
    public var sizeSystem: String?
    /** Category-specific facets from a per-item DETAIL call (e.g. Facebook's  Age Range/Character/Age Group), keyed the same way eBay item-specifics  are: aspect name -> value array. Only set by platforms with a genuine  per-item attribute source — costs one extra call per listing, so  populated by a dedicated enrichment pass, not the main list mapper.  Maps onto listings.itemSpecifics; inventory_items has no equivalent  column. */
    public var itemSpecifics: AnyCodable?
    /** When the listing was first published on the platform. Used by  the Advanced filter's listedAfter / listedBefore knobs. Optional  because not every platform returns it on the listing endpoint. */
    public var listedAt: Date?
    /** Units this platform reports. Only meaningful for platforms  `PLATFORM_QUANTITY_SYNC` marks 'native' — relist platforms show one item  and say 1 forever, so they leave this undefined rather than voting with  a number they cannot actually express. See `_quantity.ts`. */
    public var quantity: Double?
    /** Which of the user's connected accounts on this platform this listing  was scraped from — stamped by fetchCookieListings as it loops each  connected account (see listActiveForPlatform in  user-platform-accounts/read.ts). Undefined for API-track platforms  (single connection, no multi-account concept) and for any cookie path  that hasn't been threaded yet; importOne treats undefined as slot 1,  matching the historical single-account default. */
    public var accountSlot: Double?
    /** Total item weight in ounces (already summed, not split lb+oz —  the write path converts to the DB's weightLb+weightOz split). */
    public var weightOz: Double?
    public var dimensionLIn: Double?
    public var dimensionWIn: Double?
    public var dimensionHIn: Double?
    /** Max days the platform's own listing commits to ship within (eBay's  DispatchTimeMax, Etsy's processing_max). */
    public var handlingTimeDays: Double?
    public var bestOfferAutoAcceptCents: Double?
    public var bestOfferAutoDeclineCents: Double?
    /** Free text as the platform itself expresses it (e.g. eBay's `Location`  is a single seller-typed string like \"Austin, TX\", not a structured  address) — never parsed into city/state. */
    public var itemLocation: String?
    public var itemLocationZip: String?
    public var itemLocationCountry: String?
    /** What the platform's OWN listing declares shipping costs — reference  only, distinct from a realized post-sale shipping cost. */
    public var declaredShippingCost: Double?
    public var returnPolicyText: String?

    public init(platformListingId: String, title: String, description: String? = nil, price: Double? = nil, images: [String]? = nil, brand: String? = nil, condition: String? = nil, size: String? = nil, color: [String]? = nil, sku: String? = nil, url: String? = nil, category: [String]? = nil, categoryId: String? = nil, tags: [String]? = nil, material: String? = nil, style: String? = nil, pattern: String? = nil, department: String? = nil, gender: String? = nil, itemType: String? = nil, sizeSystem: String? = nil, itemSpecifics: AnyCodable? = nil, listedAt: Date? = nil, quantity: Double? = nil, accountSlot: Double? = nil, weightOz: Double? = nil, dimensionLIn: Double? = nil, dimensionWIn: Double? = nil, dimensionHIn: Double? = nil, handlingTimeDays: Double? = nil, bestOfferAutoAcceptCents: Double? = nil, bestOfferAutoDeclineCents: Double? = nil, itemLocation: String? = nil, itemLocationZip: String? = nil, itemLocationCountry: String? = nil, declaredShippingCost: Double? = nil, returnPolicyText: String? = nil) {
        self.platformListingId = platformListingId
        self.title = title
        self.description = description
        self.price = price
        self.images = images
        self.brand = brand
        self.condition = condition
        self.size = size
        self.color = color
        self.sku = sku
        self.url = url
        self.category = category
        self.categoryId = categoryId
        self.tags = tags
        self.material = material
        self.style = style
        self.pattern = pattern
        self.department = department
        self.gender = gender
        self.itemType = itemType
        self.sizeSystem = sizeSystem
        self.itemSpecifics = itemSpecifics
        self.listedAt = listedAt
        self.quantity = quantity
        self.accountSlot = accountSlot
        self.weightOz = weightOz
        self.dimensionLIn = dimensionLIn
        self.dimensionWIn = dimensionWIn
        self.dimensionHIn = dimensionHIn
        self.handlingTimeDays = handlingTimeDays
        self.bestOfferAutoAcceptCents = bestOfferAutoAcceptCents
        self.bestOfferAutoDeclineCents = bestOfferAutoDeclineCents
        self.itemLocation = itemLocation
        self.itemLocationZip = itemLocationZip
        self.itemLocationCountry = itemLocationCountry
        self.declaredShippingCost = declaredShippingCost
        self.returnPolicyText = returnPolicyText
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case platformListingId
        case title
        case description
        case price
        case images
        case brand
        case condition
        case size
        case color
        case sku
        case url
        case category
        case categoryId
        case tags
        case material
        case style
        case pattern
        case department
        case gender
        case itemType
        case sizeSystem
        case itemSpecifics
        case listedAt
        case quantity
        case accountSlot
        case weightOz
        case dimensionLIn
        case dimensionWIn
        case dimensionHIn
        case handlingTimeDays
        case bestOfferAutoAcceptCents
        case bestOfferAutoDeclineCents
        case itemLocation
        case itemLocationZip
        case itemLocationCountry
        case declaredShippingCost
        case returnPolicyText
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(platformListingId, forKey: .platformListingId)
        try container.encode(title, forKey: .title)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(price, forKey: .price)
        try container.encodeIfPresent(images, forKey: .images)
        try container.encodeIfPresent(brand, forKey: .brand)
        try container.encodeIfPresent(condition, forKey: .condition)
        try container.encodeIfPresent(size, forKey: .size)
        try container.encodeIfPresent(color, forKey: .color)
        try container.encodeIfPresent(sku, forKey: .sku)
        try container.encodeIfPresent(url, forKey: .url)
        try container.encodeIfPresent(category, forKey: .category)
        try container.encodeIfPresent(categoryId, forKey: .categoryId)
        try container.encodeIfPresent(tags, forKey: .tags)
        try container.encodeIfPresent(material, forKey: .material)
        try container.encodeIfPresent(style, forKey: .style)
        try container.encodeIfPresent(pattern, forKey: .pattern)
        try container.encodeIfPresent(department, forKey: .department)
        try container.encodeIfPresent(gender, forKey: .gender)
        try container.encodeIfPresent(itemType, forKey: .itemType)
        try container.encodeIfPresent(sizeSystem, forKey: .sizeSystem)
        try container.encodeIfPresent(itemSpecifics, forKey: .itemSpecifics)
        try container.encodeIfPresent(listedAt, forKey: .listedAt)
        try container.encodeIfPresent(quantity, forKey: .quantity)
        try container.encodeIfPresent(accountSlot, forKey: .accountSlot)
        try container.encodeIfPresent(weightOz, forKey: .weightOz)
        try container.encodeIfPresent(dimensionLIn, forKey: .dimensionLIn)
        try container.encodeIfPresent(dimensionWIn, forKey: .dimensionWIn)
        try container.encodeIfPresent(dimensionHIn, forKey: .dimensionHIn)
        try container.encodeIfPresent(handlingTimeDays, forKey: .handlingTimeDays)
        try container.encodeIfPresent(bestOfferAutoAcceptCents, forKey: .bestOfferAutoAcceptCents)
        try container.encodeIfPresent(bestOfferAutoDeclineCents, forKey: .bestOfferAutoDeclineCents)
        try container.encodeIfPresent(itemLocation, forKey: .itemLocation)
        try container.encodeIfPresent(itemLocationZip, forKey: .itemLocationZip)
        try container.encodeIfPresent(itemLocationCountry, forKey: .itemLocationCountry)
        try container.encodeIfPresent(declaredShippingCost, forKey: .declaredShippingCost)
        try container.encodeIfPresent(returnPolicyText, forKey: .returnPolicyText)
    }
}

