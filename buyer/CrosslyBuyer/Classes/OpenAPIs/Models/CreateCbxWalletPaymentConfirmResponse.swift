//
// CreateCbxWalletPaymentConfirmResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateCbxWalletPaymentConfirmResponse: Codable, JSONEncodable, Hashable {

    public enum ReleaseDecision: String, Codable, CaseIterable {
        case review = "review"
        case release = "release"
        case refuse = "refuse"
    }
    public var paymentId: String
    public var duplicate: Bool
    public var releaseDecision: ReleaseDecision
    public var releaseReason: String
    public var baseUnits: String
    public var valueCents: Double
    public var centsPerToken: Double
    public var payerAddress: String
    public var riskLevel: String?
    public var riskExposures: [String]
    public var screeningProvider: String?

    public init(paymentId: String, duplicate: Bool, releaseDecision: ReleaseDecision, releaseReason: String, baseUnits: String, valueCents: Double, centsPerToken: Double, payerAddress: String, riskLevel: String? = nil, riskExposures: [String], screeningProvider: String? = nil) {
        self.paymentId = paymentId
        self.duplicate = duplicate
        self.releaseDecision = releaseDecision
        self.releaseReason = releaseReason
        self.baseUnits = baseUnits
        self.valueCents = valueCents
        self.centsPerToken = centsPerToken
        self.payerAddress = payerAddress
        self.riskLevel = riskLevel
        self.riskExposures = riskExposures
        self.screeningProvider = screeningProvider
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case paymentId
        case duplicate
        case releaseDecision
        case releaseReason
        case baseUnits
        case valueCents
        case centsPerToken
        case payerAddress
        case riskLevel
        case riskExposures
        case screeningProvider
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(paymentId, forKey: .paymentId)
        try container.encode(duplicate, forKey: .duplicate)
        try container.encode(releaseDecision, forKey: .releaseDecision)
        try container.encode(releaseReason, forKey: .releaseReason)
        try container.encode(baseUnits, forKey: .baseUnits)
        try container.encode(valueCents, forKey: .valueCents)
        try container.encode(centsPerToken, forKey: .centsPerToken)
        try container.encode(payerAddress, forKey: .payerAddress)
        try container.encodeIfPresent(riskLevel, forKey: .riskLevel)
        try container.encode(riskExposures, forKey: .riskExposures)
        try container.encodeIfPresent(screeningProvider, forKey: .screeningProvider)
    }
}

