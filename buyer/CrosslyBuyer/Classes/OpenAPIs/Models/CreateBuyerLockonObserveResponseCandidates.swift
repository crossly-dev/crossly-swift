//
// CreateBuyerLockonObserveResponseCandidates.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateBuyerLockonObserveResponseCandidates: Codable, JSONEncodable, Hashable {

    /** Where it came from, so a confirmation can be validated. */
    public var identifierNs: String
    public var identifierValue: String
    public var title: String
    public var imageUrl: String?
    public var priceCents: Double?
    public var storeName: String?

    public init(identifierNs: String, identifierValue: String, title: String, imageUrl: String? = nil, priceCents: Double? = nil, storeName: String? = nil) {
        self.identifierNs = identifierNs
        self.identifierValue = identifierValue
        self.title = title
        self.imageUrl = imageUrl
        self.priceCents = priceCents
        self.storeName = storeName
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case identifierNs
        case identifierValue
        case title
        case imageUrl
        case priceCents
        case storeName
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(identifierNs, forKey: .identifierNs)
        try container.encode(identifierValue, forKey: .identifierValue)
        try container.encode(title, forKey: .title)
        try container.encodeIfPresent(imageUrl, forKey: .imageUrl)
        try container.encodeIfPresent(priceCents, forKey: .priceCents)
        try container.encodeIfPresent(storeName, forKey: .storeName)
    }
}

