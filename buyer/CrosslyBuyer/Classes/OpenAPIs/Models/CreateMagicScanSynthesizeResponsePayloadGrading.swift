//
// CreateMagicScanSynthesizeResponsePayloadGrading.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Third-party grading read off a slab label, when the item is one.    SEPARATE FROM &#x60;condition&#x60; and never derived from it. A grade is a claim  about what a grading company certified; inferring \&quot;PSA 10\&quot; from a coarse  condition would be inventing an authenticity claim. It is equally never  filled from our own vision estimate (&#x60;bulk_market_items.grade&#x60;), which  ships with an explicit \&quot;not a professional grade\&quot; disclaimer.    Feeds eBay&#39;s Professional Grader / Grade / Certification Number condition  descriptors (synthesize/condition-descriptors.ts) and, on platforms with  no grading field, the title. */
public struct CreateMagicScanSynthesizeResponsePayloadGrading: Codable, JSONEncodable, Hashable {

    public var graderSlug: String
    /** As the grader writes it: '10', '9.8', 'MS-65'. */
    public var grade: String
    public var qualifier: String?
    /** Second axis on a `dual` scale — WATA's seal grade. */
    public var sealGrade: String?
    public var certNumber: String?
    /** Derived. Never assign by hand — call `gradeKey()`. */
    public var gradeKey: String
    public var verifiedAt: String?

    public init(graderSlug: String, grade: String, qualifier: String? = nil, sealGrade: String? = nil, certNumber: String? = nil, gradeKey: String, verifiedAt: String? = nil) {
        self.graderSlug = graderSlug
        self.grade = grade
        self.qualifier = qualifier
        self.sealGrade = sealGrade
        self.certNumber = certNumber
        self.gradeKey = gradeKey
        self.verifiedAt = verifiedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case graderSlug
        case grade
        case qualifier
        case sealGrade
        case certNumber
        case gradeKey
        case verifiedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(graderSlug, forKey: .graderSlug)
        try container.encode(grade, forKey: .grade)
        try container.encodeIfPresent(qualifier, forKey: .qualifier)
        try container.encodeIfPresent(sealGrade, forKey: .sealGrade)
        try container.encodeIfPresent(certNumber, forKey: .certNumber)
        try container.encode(gradeKey, forKey: .gradeKey)
        try container.encodeIfPresent(verifiedAt, forKey: .verifiedAt)
    }
}

