//
// GetOrderEvidenceResponseGradeSignals.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetOrderEvidenceResponseGradeSignals: Codable, JSONEncodable, Hashable {

    public enum Key: String, Codable, CaseIterable {
        case packingCapture = "packing_capture"
        case continuousTake = "continuous_take"
        case sealVerified = "seal_verified"
        case serialPreRecorded = "serial_pre_recorded"
        case serialSeenOnFrame = "serial_seen_on_frame"
        case arrivalCondition = "arrival_condition"
    }
    public var key: Key
    public var points: Double
    /** Shown to the seller verbatim. Must read as a fact, not a verdict. */
    public var detail: String

    public init(key: Key, points: Double, detail: String) {
        self.key = key
        self.points = points
        self.detail = detail
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case key
        case points
        case detail
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(key, forKey: .key)
        try container.encode(points, forKey: .points)
        try container.encode(detail, forKey: .detail)
    }
}

