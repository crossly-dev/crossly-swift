//
// GetSpatialSceneResponseBreakdowns.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetSpatialSceneResponseBreakdowns: Codable, JSONEncodable, Hashable {

    public enum Facet: String, Codable, CaseIterable {
        case brand = "brand"
        case condition = "condition"
        case size = "size"
        case status = "status"
        case label = "label"
        case title = "title"
        case value = "value"
        case year = "year"
        case grade = "grade"
        case _set = "set"
        case franchise = "franchise"
        case game = "game"
        case team = "team"
        case era = "era"
        case genre = "genre"
        case colour = "colour"
        case age = "age"
    }
    public var facet: Facet
    public var distinct: Double

    public init(facet: Facet, distinct: Double) {
        self.facet = facet
        self.distinct = distinct
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case facet
        case distinct
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(facet, forKey: .facet)
        try container.encode(distinct, forKey: .distinct)
    }
}

