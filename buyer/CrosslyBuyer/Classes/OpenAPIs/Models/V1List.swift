//
// V1List.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The canonical collection envelope. The array is ALWAYS under &#x60;data&#x60;, never under the noun — so one accessor works for every list endpoint. */
public struct V1List: Codable, JSONEncodable, Hashable {

    /** The rows. */
    public var data: [AnyCodable]
    public var pagination: Pagination?
    /** Endpoint-specific extras that are NOT pagination — e.g. `pendingCents`, `basis`. Omitted when empty. */
    public var meta: [String: AnyCodable]?

    public init(data: [AnyCodable], pagination: Pagination? = nil, meta: [String: AnyCodable]? = nil) {
        self.data = data
        self.pagination = pagination
        self.meta = meta
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case data
        case pagination
        case meta
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(data, forKey: .data)
        try container.encodeIfPresent(pagination, forKey: .pagination)
        try container.encodeIfPresent(meta, forKey: .meta)
    }
}

