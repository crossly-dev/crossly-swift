//
// GetVariationGroupPublishPlanResponsePlans.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetVariationGroupPublishPlanResponsePlans: Codable, JSONEncodable, Hashable {

    public enum Mode: String, Codable, CaseIterable {
        case grouped = "grouped"
        case separate = "separate"
    }
    public var platform: String
    public var mode: Mode
    /** How many marketplace listings this produces. One when grouped; one per  option when separate — the number the seller should see before they  commit, not after. */
    public var listingsProduced: Double
    /** Plain-language, shown in the publish dialog. */
    public var explanation: String

    public init(platform: String, mode: Mode, listingsProduced: Double, explanation: String) {
        self.platform = platform
        self.mode = mode
        self.listingsProduced = listingsProduced
        self.explanation = explanation
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case platform
        case mode
        case listingsProduced
        case explanation
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(platform, forKey: .platform)
        try container.encode(mode, forKey: .mode)
        try container.encode(listingsProduced, forKey: .listingsProduced)
        try container.encode(explanation, forKey: .explanation)
    }
}

