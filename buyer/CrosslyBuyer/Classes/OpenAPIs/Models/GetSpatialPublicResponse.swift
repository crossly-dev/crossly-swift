//
// GetSpatialPublicResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetSpatialPublicResponse: Codable, JSONEncodable, Hashable {

    public var scene: GetSpatialPublicResponseScene
    public var profile: GetSpatialPublicResponseProfile
    public var solved: GetSpatialPublicResponseSolved
    /** Enough to draw an object and label it. No cost, no location, no status. */
    public var items: [GetSpatialPublicResponseItems]
    public var stats: GetSpatialPublicResponseStats

    public init(scene: GetSpatialPublicResponseScene, profile: GetSpatialPublicResponseProfile, solved: GetSpatialPublicResponseSolved, items: [GetSpatialPublicResponseItems], stats: GetSpatialPublicResponseStats) {
        self.scene = scene
        self.profile = profile
        self.solved = solved
        self.items = items
        self.stats = stats
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case scene
        case profile
        case solved
        case items
        case stats
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(scene, forKey: .scene)
        try container.encode(profile, forKey: .profile)
        try container.encode(solved, forKey: .solved)
        try container.encode(items, forKey: .items)
        try container.encode(stats, forKey: .stats)
    }
}

