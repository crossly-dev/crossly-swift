//
// GetAiStatusResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetAiStatusResponse: Codable, JSONEncodable, Hashable {

    public enum ActiveProvider: String, Codable, CaseIterable {
        case custom = "custom"
        case openai = "openai"
        case anthropic = "anthropic"
        case google = "google"
        case groq = "groq"
        case openrouter = "openrouter"
        case xai = "xai"
        case together = "together"
    }
    public var hasKey: Bool
    public var aiEnabled: Bool
    public var activeProvider: ActiveProvider
    public var providers: AnyCodable
    public var capabilities: GetAiStatusResponseCapabilities
    public var customConfig: GetAiStatusResponseCustomConfig?

    public init(hasKey: Bool, aiEnabled: Bool, activeProvider: ActiveProvider, providers: AnyCodable, capabilities: GetAiStatusResponseCapabilities, customConfig: GetAiStatusResponseCustomConfig? = nil) {
        self.hasKey = hasKey
        self.aiEnabled = aiEnabled
        self.activeProvider = activeProvider
        self.providers = providers
        self.capabilities = capabilities
        self.customConfig = customConfig
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case hasKey
        case aiEnabled
        case activeProvider
        case providers
        case capabilities
        case customConfig
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(hasKey, forKey: .hasKey)
        try container.encode(aiEnabled, forKey: .aiEnabled)
        try container.encode(activeProvider, forKey: .activeProvider)
        try container.encode(providers, forKey: .providers)
        try container.encode(capabilities, forKey: .capabilities)
        try container.encodeIfPresent(customConfig, forKey: .customConfig)
    }
}

