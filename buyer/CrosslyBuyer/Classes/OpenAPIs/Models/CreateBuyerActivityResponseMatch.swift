//
// CreateBuyerActivityResponseMatch.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateBuyerActivityResponseMatch: Codable, JSONEncodable, Hashable {

    public enum Verdict: String, Codable, CaseIterable {
        case cheaper = "cheaper"
        case notCheaper = "not_cheaper"
        case noPagePrice = "no_page_price"
        case currencyMismatch = "currency_mismatch"
    }
    public var verdict: Verdict
    public var offer: CreateBuyerActivityResponseMatchOffer
    /** Positive when cheaper. Null when no page price was supplied. */
    public var savingCents: Double?
    /** TRUE means the offer's condition and the page's are comparable. False means we matched the item but not its state — a used Crossly copy against a retailer's new one — and the UI must say so rather than claim a saving. */
    public var conditionComparable: Bool
    /** Always false today. Crossly shipping is computed at checkout from the buyer's address, which Scout does not have and should not send. Present so the surface that renders \"before shipping\" is reading a fact rather than hard-coding an assumption that stops being true when we add it. */
    public var shippingKnown: Bool
    public var catalog: GetCatalogLookupResponseCatalog?
    /** Other buyable offers for the SAME item, cheapest first, best excluded.  Deliberately not \"you might also like\". We have no behavioural data to build that from, and inventing it would put unrelated items under a badge whose entire value is that it only ever appears when we have the thing the buyer is actually looking at.  What these ARE is the same product from other sellers, in other conditions, at other prices — which is the choice a buyer on a product page genuinely wants, and the one a single \"cheapest\" result hides. */
    public var alternates: [CreateBuyerActivityResponseMatchOffer]

    public init(verdict: Verdict, offer: CreateBuyerActivityResponseMatchOffer, savingCents: Double? = nil, conditionComparable: Bool, shippingKnown: Bool, catalog: GetCatalogLookupResponseCatalog? = nil, alternates: [CreateBuyerActivityResponseMatchOffer]) {
        self.verdict = verdict
        self.offer = offer
        self.savingCents = savingCents
        self.conditionComparable = conditionComparable
        self.shippingKnown = shippingKnown
        self.catalog = catalog
        self.alternates = alternates
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case verdict
        case offer
        case savingCents
        case conditionComparable
        case shippingKnown
        case catalog
        case alternates
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(verdict, forKey: .verdict)
        try container.encode(offer, forKey: .offer)
        try container.encodeIfPresent(savingCents, forKey: .savingCents)
        try container.encode(conditionComparable, forKey: .conditionComparable)
        try container.encode(shippingKnown, forKey: .shippingKnown)
        try container.encodeIfPresent(catalog, forKey: .catalog)
        try container.encode(alternates, forKey: .alternates)
    }
}

