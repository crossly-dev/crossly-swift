//
// GetOrderEvidenceResponseGrade.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetOrderEvidenceResponseGrade: Codable, JSONEncodable, Hashable {

    public enum Grade: String, Codable, CaseIterable {
        case _none = "none"
        case strong = "strong"
        case basic = "basic"
        case conclusive = "conclusive"
    }
    public var grade: Grade
    public var points: Double
    public var signals: [GetOrderEvidenceResponseGradeSignals]
    /** The single most valuable thing not yet done, phrased as an action.  One, not a checklist. A seller given six things to fix does none of them; a seller given the one worth the most does that one. */
    public var nextStep: String?

    public init(grade: Grade, points: Double, signals: [GetOrderEvidenceResponseGradeSignals], nextStep: String? = nil) {
        self.grade = grade
        self.points = points
        self.signals = signals
        self.nextStep = nextStep
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case grade
        case points
        case signals
        case nextStep
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(grade, forKey: .grade)
        try container.encode(points, forKey: .points)
        try container.encode(signals, forKey: .signals)
        try container.encodeIfPresent(nextStep, forKey: .nextStep)
    }
}

