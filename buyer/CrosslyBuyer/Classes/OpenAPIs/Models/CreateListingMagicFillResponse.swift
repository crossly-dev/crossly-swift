//
// CreateListingMagicFillResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateListingMagicFillResponse: Codable, JSONEncodable, Hashable {

    public var filled: AnyCodable
    public var count: Double
    /** False when the seller has no AI provider configured — `count: 0` then means \"nothing new could be resolved\" rather than \"already complete\", a distinction the caller (web's useMagicFill toast) needs but `count` alone can't express. True whenever a provider IS configured, even if the AI call itself failed for some other reason (network, bad key) — that's a different problem than \"connect a provider.\" */
    public var aiAvailable: Bool

    public init(filled: AnyCodable, count: Double, aiAvailable: Bool) {
        self.filled = filled
        self.count = count
        self.aiAvailable = aiAvailable
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case filled
        case count
        case aiAvailable
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(filled, forKey: .filled)
        try container.encode(count, forKey: .count)
        try container.encode(aiAvailable, forKey: .aiAvailable)
    }
}

