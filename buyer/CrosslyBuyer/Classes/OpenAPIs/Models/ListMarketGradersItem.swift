//
// ListMarketGradersItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ListMarketGradersItem: Codable, JSONEncodable, Hashable {

    public enum Scale: String, Codable, CaseIterable {
        case numeric = "numeric"
        case sheldon = "sheldon"
        case dual = "dual"
        case hundred = "hundred"
    }
    public var slug: String
    public var name: String
    public var categorySlugs: [String]
    public var scale: Scale
    public var grades: [String]
    public var qualifiers: [String]
    public var sealGrades: [String]

    public init(slug: String, name: String, categorySlugs: [String], scale: Scale, grades: [String], qualifiers: [String], sealGrades: [String]) {
        self.slug = slug
        self.name = name
        self.categorySlugs = categorySlugs
        self.scale = scale
        self.grades = grades
        self.qualifiers = qualifiers
        self.sealGrades = sealGrades
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case slug
        case name
        case categorySlugs
        case scale
        case grades
        case qualifiers
        case sealGrades
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
        try container.encode(categorySlugs, forKey: .categorySlugs)
        try container.encode(scale, forKey: .scale)
        try container.encode(grades, forKey: .grades)
        try container.encode(qualifiers, forKey: .qualifiers)
        try container.encode(sealGrades, forKey: .sealGrades)
    }
}

