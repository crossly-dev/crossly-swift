//
// CreateListingCheckDuplicateResponseMatches.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateListingCheckDuplicateResponseMatches: Codable, JSONEncodable, Hashable {

    public enum Suggested: String, Codable, CaseIterable {
        case variation = "variation"
        case restock = "restock"
        case duplicate = "duplicate"
    }
    public enum MatchType: String, Codable, CaseIterable {
        case title = "title"
        case image = "image"
    }
    /** What we'd offer to do about this match. A suggestion for which button to  feature — never a decision. All three actions stay available. */
    public var suggested: Suggested
    /** The variation group to ADD to, when the match already belongs to one.  Null means there is no group yet and choosing `variation` creates one from  the match plus the new listing. Without this the UI has to guess, and  guessing wrong means either a second group beside the first or a silent  no-op. */
    public var variationGroupId: String?
    /** The seller's existing listing this scan probably duplicates (null if the  match landed only on an inventory item with no listing row). */
    public var listingId: String?
    /** The inventory item behind that listing, when linked. Drives the  \"View inventory\" button. */
    public var inventoryItemId: String?
    public var title: String
    public var imageUrl: String?
    public var matchType: MatchType
    /** 0–1 confidence. Image matches report 1; title matches the similarity. */
    public var score: Double

    public init(suggested: Suggested, variationGroupId: String? = nil, listingId: String? = nil, inventoryItemId: String? = nil, title: String, imageUrl: String? = nil, matchType: MatchType, score: Double) {
        self.suggested = suggested
        self.variationGroupId = variationGroupId
        self.listingId = listingId
        self.inventoryItemId = inventoryItemId
        self.title = title
        self.imageUrl = imageUrl
        self.matchType = matchType
        self.score = score
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case suggested
        case variationGroupId
        case listingId
        case inventoryItemId
        case title
        case imageUrl
        case matchType
        case score
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(suggested, forKey: .suggested)
        try container.encodeIfPresent(variationGroupId, forKey: .variationGroupId)
        try container.encodeIfPresent(listingId, forKey: .listingId)
        try container.encodeIfPresent(inventoryItemId, forKey: .inventoryItemId)
        try container.encode(title, forKey: .title)
        try container.encodeIfPresent(imageUrl, forKey: .imageUrl)
        try container.encode(matchType, forKey: .matchType)
        try container.encode(score, forKey: .score)
    }
}

