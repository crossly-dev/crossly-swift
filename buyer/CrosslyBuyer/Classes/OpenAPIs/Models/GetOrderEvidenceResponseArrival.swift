//
// GetOrderEvidenceResponseArrival.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetOrderEvidenceResponseArrival: Codable, JSONEncodable, Hashable {

    public enum State: String, Codable, CaseIterable {
        case notRequested = "not_requested"
        case awaiting = "awaiting"
        case submitted = "submitted"
        case declined = "declined"
        case windowClosed = "window_closed"
    }
    public var state: State
    public var windowHours: Double
    public var requestedAt: Date?
    /** When the window shuts. Null when nothing was ever asked. */
    public var closesAt: Date?
    public var detail: String
    public var photos: [String]

    public init(state: State, windowHours: Double, requestedAt: Date? = nil, closesAt: Date? = nil, detail: String, photos: [String]) {
        self.state = state
        self.windowHours = windowHours
        self.requestedAt = requestedAt
        self.closesAt = closesAt
        self.detail = detail
        self.photos = photos
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case state
        case windowHours
        case requestedAt
        case closesAt
        case detail
        case photos
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(state, forKey: .state)
        try container.encode(windowHours, forKey: .windowHours)
        try container.encodeIfPresent(requestedAt, forKey: .requestedAt)
        try container.encodeIfPresent(closesAt, forKey: .closesAt)
        try container.encode(detail, forKey: .detail)
        try container.encode(photos, forKey: .photos)
    }
}

