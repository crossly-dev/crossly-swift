//
// CreateCbxRedemptionQuoteResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateCbxRedemptionQuoteResponse: Codable, JSONEncodable, Hashable {

    public enum ServiceKind: String, Codable, CaseIterable {
        case grading = "grading"
        case subscription = "subscription"
        case authentication = "authentication"
        case evidenceSeal = "evidence_seal"
    }
    public var serviceKind: ServiceKind
    public var label: String
    public var listPriceCents: Double
    public var chargedCents: Double
    public var discountBps: Double
    public var baseUnits: String
    public var centsPerToken: Double

    public init(serviceKind: ServiceKind, label: String, listPriceCents: Double, chargedCents: Double, discountBps: Double, baseUnits: String, centsPerToken: Double) {
        self.serviceKind = serviceKind
        self.label = label
        self.listPriceCents = listPriceCents
        self.chargedCents = chargedCents
        self.discountBps = discountBps
        self.baseUnits = baseUnits
        self.centsPerToken = centsPerToken
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case serviceKind
        case label
        case listPriceCents
        case chargedCents
        case discountBps
        case baseUnits
        case centsPerToken
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(serviceKind, forKey: .serviceKind)
        try container.encode(label, forKey: .label)
        try container.encode(listPriceCents, forKey: .listPriceCents)
        try container.encode(chargedCents, forKey: .chargedCents)
        try container.encode(discountBps, forKey: .discountBps)
        try container.encode(baseUnits, forKey: .baseUnits)
        try container.encode(centsPerToken, forKey: .centsPerToken)
    }
}

