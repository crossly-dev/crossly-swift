//
// ListSpatialPublicOffersItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ListSpatialPublicOffersItem: Codable, JSONEncodable, Hashable {

    public var itemId: String
    /** platform_listings.platform_listing_id — what POST /cart/items takes. */
    public var listingSlug: String
    public var priceCents: Double
    public var currency: String
    /** Remaining stock, when the listing declares one. Null = unknown. */
    public var available: Double?
    /** What a buyer is entitled to know before they add it. */
    public var condition: String?
    /** Canonical grade key when the item is slabbed, e.g. `psa-10`. */
    public var gradeKey: String?

    public init(itemId: String, listingSlug: String, priceCents: Double, currency: String, available: Double? = nil, condition: String? = nil, gradeKey: String? = nil) {
        self.itemId = itemId
        self.listingSlug = listingSlug
        self.priceCents = priceCents
        self.currency = currency
        self.available = available
        self.condition = condition
        self.gradeKey = gradeKey
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case itemId
        case listingSlug
        case priceCents
        case currency
        case available
        case condition
        case gradeKey
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(itemId, forKey: .itemId)
        try container.encode(listingSlug, forKey: .listingSlug)
        try container.encode(priceCents, forKey: .priceCents)
        try container.encode(currency, forKey: .currency)
        try container.encodeIfPresent(available, forKey: .available)
        try container.encodeIfPresent(condition, forKey: .condition)
        try container.encodeIfPresent(gradeKey, forKey: .gradeKey)
    }
}

